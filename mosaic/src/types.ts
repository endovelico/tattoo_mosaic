export interface TilePosition {
  x: number;
  y: number;
  width: number;
  height: number;
  rotation: number;
  scale: number;
  depth: number;
}

export interface ImageData {
  id: number;
  src: string;
}
